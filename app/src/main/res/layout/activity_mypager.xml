<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/my_pager_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/img_view"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintBottom_toBottomOf="@+id/my_pager_parent"
        app:layout_constraintLeft_toLeftOf="@+id/my_pager_parent"
        app:layout_constraintRight_toRightOf="@+id/my_pager_parent"
        app:layout_constraintTop_toTopOf="@+id/my_pager_parent"/>

</android.support.constraint.ConstraintLayout>